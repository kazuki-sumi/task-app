<%= form_with model: [:admin, article_file], local: true do |f| %>

  <div class="form-group">
    <%= f.file_field :file, class: "form-control-file" %>
  </div>

  <div class="form-row">
    <%= f.submit "作成", class: "btn btn-primary" %>
  </div>
<% end %>
